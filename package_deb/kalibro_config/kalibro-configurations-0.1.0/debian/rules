#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --buildsystem=ruby --with ruby

override_dh_auto_install:
        $(MAKE) DESTDIR=$$(pwd)/debian/kalibro_configuration prefix=/usr install

